@page
@model Proiect_MDP_Web.Pages.Firme.IndexModel

@{
    ViewData["Title"] = "Firme";
}

<h1>Firme</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Firma[0].DenumireFirma)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Firma[0].Locatie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Firma) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DenumireFirma)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Locatie)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
